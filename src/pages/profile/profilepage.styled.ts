import styled from 'styled-components';

export const StyledProfilePageClose = styled.div`
    gap: 30px 0px;
    & p:first-child {
        font-weight: 700;
        font-size: 25px;
    }
    & p:last-child {
        gap: 0;
        font-size: 14px;
    }
`;
